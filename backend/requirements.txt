Django==5.2.17
djangorestframework==3.16.1
django-cors-headers==4.9.0
django-environ==0.12.1
django-filter==25.2

# Recurrence arithmetic: month-length clamping and DST-safe intervals.
python-dateutil==2.9.0.post0

# Reading the shop-rental form's responses sheet (apps/shoprentals/sheets.py).
# Imported lazily there, so the portal runs without a Google setup — but the
# sync command needs these, and production runs the sync.
google-api-python-client==2.198.0
google-auth==2.56.3

# Production serving. Not needed for `runserver`, but importing config.settings.prod
# requires whitenoise, so it stays in the base requirements.
gunicorn==23.0.0
whitenoise==6.11.0
psycopg[binary]==3.2.12
